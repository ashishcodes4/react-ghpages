{
  "name": "ghpages-react",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://github.com/ashishcodes4/react-ghpages",
  "dependencies": {
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "ghpages -d build"
  },
  "devDependencies": {
    "gh-pages": "^1.2.0"
  }
}
